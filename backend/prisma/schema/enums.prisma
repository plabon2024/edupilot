enum Role {
  USER
  ADMIN
}

enum UserStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
}

enum DocumentStatus {
  PROCESSING
  READY
  FAILED
}

enum Difficulty {
  EASY
  MEDIUM
  HARD
}

enum PaymentStatus {
  PENDING
  SUCCEEDED
  FAILED
  CANCELLED
}

enum PaymentType {
  SUBSCRIPTION
}

enum MessageRole {
  USER
  ASSISTANT
}