<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Redirecting...</title>
</head>

<body>
  <p>Completing sign-in...</p>
  <script>
    // Navigate to the frontend success/dashboard URL passed from the server.
    // Tokens (if any) are embedded in the hash by the server — they stay client-side only.
    window.location.replace("<%= url %>");
  </script>
</body>